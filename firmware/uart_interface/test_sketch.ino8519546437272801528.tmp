int count = 0;
int buffer[100];

void setup() {
  Serial.begin(9600);

  for (count=0; count<sizeof(buffer); count++) { // initialization
    buffer[count] = count;
  }
}

int marker0;
void loop() {
  for (marker
}

