const int N = 2;
int16_t AcX[N],AcY[N],AcZ[N],Tmp[N],GyX[N],GyY[N],GyZ[N];

struct Dataframe {
    int16_t AcX[N], AcY[N], AcZ[N], Tmp[N], GyX[N], GyY[N], GyZ[N];
} dataframe;

void setup() {
  Serial.begin(9600);
}


void loop() {
//  String toSend = "This message was sent from Arduino MEGA";
//  int strSize = sizeof(toSend);
//  Serial.println(toSend);
//  delay(1000);
  compileData();
}

void compileData() {
  // dummy values
  dataframe.AcX[0] = 100;
  dataframe.AcY[0] = 200;
  dataframe.AcZ[0] = 300;
  dataframe.Tmp[0] = 50;
  dataframe.GyX[0] = 400;
  dataframe.GyY[0] = 500;
  dataframe.GyZ[0] = 600;

  dataframe.AcX[1] = 1000;
  dataframe.AcY[1] = 2000;
  dataframe.AcZ[1] = 3000;
  dataframe.Tmp[1] = 100;
  dataframe.GyX[1] = 4000;
  dataframe.GyY[1] = 5000;
  dataframe.GyZ[1] = 6000;

  char sensor_one[100];
  char sensor_two[100];
  char AcX_one = (char) dataframe.AcX[0];
  char AcY_one = (char) dataframe.AcY[0];
  char AcZ_one = (char) dataframe.AcZ[0];
  char Tmp_one = (char) dataframe.Tmp[0];
  char GyX_one = (char) dataframe.GyX[0];
  char GyY_one = (char) dataframe.GyY[0];
  char GyZ_one = (char) dataframe.GyZ[0];
  strcpy(sensor_one, &AcX_one);
  strcpy(sensor_one, );
  strcpy(sensor_one, );
  strcpy(sensor_one, );
  strcpy(sensor_one, );
  strcpy(sensor_one, );
  strcpy(sensor_one, );
  
  strcpy(sensor_two, dataframe.AcX[1]);
  strcpy(sensor_two, dataframe.AcY[1]);
  strcpy(sensor_two, dataframe.AcZ[1]);
  strcpy(sensor_two, dataframe.Tmp[1]);
  strcpy(sensor_two, dataframe.GyX[1]);
  strcpy(sensor_two, dataframe.GyY[1]);
  strcpy(sensor_two, dataframe.GyZ[1]);

  Serial.println(sensor_one);
  delay(500);
  Serial.println(sensor_two);
  delay(500);
}

